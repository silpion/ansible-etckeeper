---
# lookup role version
etckeeper_role_version: "{{ lookup('file', role_path + '/VERSION') }}"

etckeeper_supported_platforms:
  - Debian
  - RedHat

etckeeper_supported_vcs:
  - git
  - mercurial
  - bzr
  - darcs
